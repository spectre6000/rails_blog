<% provide( :title, "Author Log in" ) %>
<h1>Author Log in</h1>

<div>
  <div>
    <%= form_for( :session, url: author_login_path ) do | f | %>
      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "Log in" %>
    <% end %>
  </div>
</div>